import styled from "styled-components";

export const StatusBox = styled.div`
  background: ${({ theme }) => theme.colors.gray_900};
  padding: 2.25rem;
  border-radius: 0.5rem;

  display: flex;
  flex-direction: column;
  align-items:center;

  flex: 1;
`

export const StatusBoxItem = styled.div`

`